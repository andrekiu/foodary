// @flow

import {combineReducers} from 'redux';
import type {State, Action} from '../store.flow';

//export default combineReducers({
//
//});

const reducer = (state: State, action: Action) => state;

export default reducer;
